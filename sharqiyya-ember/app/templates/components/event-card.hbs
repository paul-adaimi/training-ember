<div class="event-card" onclick={{action "showEvent"}}>
  <div onclick={{perform updateIsFavoriteTask}} class="event-card__like-button js-like-button {{if isFavorite "event-card__like-button_liked"}}"></div>
  <img src='/images/events/event-{{id}}.png' class="event-card__image" />
  <div class="event-card__details">
    <div class="event-card__from-to">
      {{dateString}}
    </div>
    <div class="event-card__title">
      {{title}}
    </div>
    <div class="event-card__timing-and-location">
      <img class="event-card__icon" src="/images/icons/time.svg">
      {{timeString}}
    </div>
    <div class="event-card__timing-and-location">
      <img class="event-card__icon" src="/images/icons/location.svg">
      {{location}}
    </div>
    <div class="event-card__tags">
      {{#each tags as |tag|}}
        <div class="event-card__tag"># {{tag.title}}</div>
      {{/each}}
    </div>
  </div>
</div>
